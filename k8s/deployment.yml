
apiVersion: apps/v1
kind: Deployment
metadata:
  name: githubactions-aks-demo
spec:
  selector:
    matchLabels:
      app: shop-api-auth
  template:
    metadata:
      labels:
        app: shop-api-auth
    spec:
      containers:
        - name: shop-api-auth
          image: jayasith98/shop-api-auth-service
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          ports:
            - containerPort: 5006